var studentGrade = lbl(4, 'S') // student grade is labeled as {S}
var teacherGrade = lbl(5, 'T') // teacher grade is labeled as {T}
var combinedGrade = teacherGrade + studentGrade // combinedGrade is now labeled as {S,T}
lprint(combinedGrade);
